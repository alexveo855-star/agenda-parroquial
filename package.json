{
  "scripts": {
    "build": "chmod +x node_modules/.bin/postcss && npx postcss ./src/input.css -o ./public/output.css && node scripts/create-config.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1"
  }
}
